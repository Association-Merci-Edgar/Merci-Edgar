%p#notice= notice

%p
  %b First name:
  = @person.first_name
%p
  %b Last name:
  = @person.name

%p
Coordonnées

- @person.addresses.each do |address|
  = render address

- @person.emails.each do |email|
  = render email

- @person.phones.each do |phone|
  = render phone

- @person.websites.each do |website|
  = render website

= link_to 'Edit', edit_venue_person_path(@person.structures.first,@person)
\|
= link_to 'Back', @person.structures.first
