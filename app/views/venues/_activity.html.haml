.row-fluid
  .span8
    -#
      .btn-group
        //ici un select :
        %a.btn.dropdown-toggle{"data-toggle" => "dropdown", :href => "#"}
          Selectionner un projet
          %span.caret
        %ul.dropdown-menu
          %li projet 1
          %li projet 2

    #activityfield-outter
      = simple_form_for(@reporting) do |f|
        = f.input :project_id, collection: Project.all, label: false, value_method: :id, prompt: "Selectionnez un projet", :input_html => { :class => ".ed-select" }
        = f.input :note_report_content, label:false, input_html: {id: "activityfield"}, placeholder: "écrire une nouvelle note", as: :text
        = f.hidden_field :asset_id
        = f.hidden_field :asset_type
        = button_tag(type: 'submit', class: "btn-secondary submit") do 
          %i.entypo.edi-arrow-down-5
                              
    / ____________________________________ list reportings
    %ul.list-large
      = render @reportings
   



  / _____________________ infos-mini  
  #infos-mini.span4
    - if @venue.relative
      %ul.list-infos
        %li.infolabel
          Contact principal
        %li
          .pic.pic-small= image_tag @venue.relative.avatar
          = link_to @venue.relative do
            %h4= @venue.relative
        %li= @venue.relative.title(@venue)
        %li= @venue.relative.phone_number
        %li= @venue.relative.email_address
  
    %ul.list-infos
      - if @venue.address
        %li.infolabel= t(@venue.address.kind, scope: "simple_form.options.venue.addresses.kind")
        %li= @venue.address.full_address
      %li= @venue.email_address
      
    %ul.list-infos
      %li.infolabel Autres
      - @venue.emails.each do |email|
        = render email

      - @venue.phones.each do |phone|
        = render phone
        
      - @venue.websites.each do |website|
        = render website
