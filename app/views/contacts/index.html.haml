= render 'header'
= render 'contacts_imports/notices' if @nb_imported_contacts.present?
/______________________________________________________ LISTS       
.progress{:style => "display:none"}
  .bar

.row-fluid
  = render 'paginated_contacts'